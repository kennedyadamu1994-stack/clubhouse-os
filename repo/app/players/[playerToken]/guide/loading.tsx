import { PageSkeleton } from "@/components/skeletons/page-skeleton";

/** See the public Guide page's own loading.tsx for the full reasoning — identical skeleton. */
export default function Loading() {
  return <PageSkeleton />;
}
